const createInCollection = (name) => {
  console.log(`${name} is added in fruit Collection`);
};

const updateInCollection = (name) => {
  console.log(`${name} object is update in fruit Collection`);
};
const deleteInCollection = (name) => {
  console.log(`${name} object is delete in fruit Collection`);
};

module.exports = { createInCollection, updateInCollection, deleteInCollection };
